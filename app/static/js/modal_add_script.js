
window.addEventListener('load', function () {
    const elemModal = document.querySelector('#addModal');
    const modal = new bootstrap.Modal(elemModal);
    modal.show();
    console.log(modal)
});
document.querySelector("#addModal").addEventListener()
